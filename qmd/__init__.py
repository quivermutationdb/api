"""qmd — Quiver Mutation Database core library (offline math pipeline)."""

# Recorded in rank_stats.pipeline_version for every exported rank. Bump on any
# change that alters stored values (invariants, exploration semantics, schema).
__version__ = "2.0.0"
